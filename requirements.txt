# Requirements for multimeida project
# Detected external dependencies from project imports.
# Notes:
# - `paddleocr` requires `paddlepaddle` (choose cpu/gpu wheel appropriate for your machine).
# - On macOS you may need to install the macOS/CPU wheel of paddlepaddle or use conda for GPU support.

requests
numpy
opencv-python
paddleocr

# paddleocr cpu
python -m pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/

# paddleocr gpu (example for CUDA 12.9)
 python -m pip install paddlepaddle-gpu==3.2.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/